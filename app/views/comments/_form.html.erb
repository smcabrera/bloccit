<%= form_for [@post.topic, @post, @post.comments.build], remote: true do |f| %>
  <%= f.text_field :body, class: 'form-control', placeholder: "New comment" %>
  <%= f.submit class: 'btn' %>
<% end %>
